<UserControl x:Class="EGO_Library.Views.Controls.RecipeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="10" Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Рецепты Слияния" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White"/>
            <Button Content="Назад" 
                    Width="80" 
                    Height="30" 
                    Command="{Binding GoBackCommand}"
                    DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- Фильтры -->
        <Border Grid.Row="1" 
                Background="#2D2D30" 
                CornerRadius="8" 
                Padding="15" 
                Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="250" 
                         Height="30"
                         Padding="8"
                         Background="#3C3C3C"
                         Foreground="White"
                         BorderBrush="#555555"
                         ToolTip="Поиск рецептов..."/>

                <ComboBox ItemsSource="{Binding AvailableDifficulties}" 
                         SelectedItem="{Binding SelectedDifficulty}"
                         Width="120" 
                         Height="30"
                         Margin="10,0,0,0"
                         Background="#3C3C3C"
                         Foreground="White"/>

                <Button Content="Сбросить" 
                        Command="{Binding ClearFiltersCommand}"
                        Width="80" 
                        Height="30"
                        Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Список рецептов -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Recipes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Карточка рецепта -->
                        <Border Background="#2D2D30" 
                                BorderBrush="#404040" 
                                BorderThickness="1"
                                CornerRadius="8" 
                                Padding="15" 
                                Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Левая часть: информация о рецепте -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" 
                                               FontSize="18" 
                                               FontWeight="Bold" 
                                               Foreground="White"/>

                                    <TextBlock Text="{Binding Description}" 
                                               TextWrapping="Wrap" 
                                               Foreground="#CCCCCC"
                                               Margin="0,5,0,10"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="Локация: " 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#AAAAAA"/>
                                        <TextBlock Text="{Binding Location}" 
                                                   Foreground="#CCCCCC"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Сложность: " 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#AAAAAA"/>
                                        <TextBlock Text="{Binding Difficulty}" 
                                                   Foreground="{Binding Difficulty, Converter={StaticResource DifficultyToColorConverter}}"/>
                                    </StackPanel>

                                    <!-- Требуемые дары -->
                                    <TextBlock Text="Необходимые дары:" 
                                               FontWeight="SemiBold" 
                                               Foreground="#AAAAAA"
                                               Margin="0,10,0,5"/>

                                    <ItemsControl ItemsSource="{Binding RequiredGifts}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#404040" 
                                                        CornerRadius="4" 
                                                        Padding="5" 
                                                        Margin="0,2">
                                                    <TextBlock Text="{Binding Name}" 
                                                               Foreground="White" 
                                                               FontSize="12"/>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>

                                <!-- Правая часть: результат -->
                                <Border Grid.Column="1" 
                                        Background="#007ACC" 
                                        CornerRadius="6" 
                                        Padding="10" 
                                        Margin="10,0,0,0"
                                        Width="150">
                                    <StackPanel>
                                        <TextBlock Text="Результат" 
                                                   FontWeight="Bold" 
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"/>

                                        <TextBlock Text="{Binding ResultGift.Name}" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="White"
                                                   TextAlignment="Center"
                                                   Margin="0,5,0,0"/>

                                        <TextBlock Text="{Binding ResultGift.Icon}" 
                                                   FontSize="20"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,5,0,0"/>

                                        <TextBlock Text="{Binding ResultGift.Tier, StringFormat='Tier {0}'}" 
                                                   FontSize="10" 
                                                   Foreground="#CCCCCC"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>