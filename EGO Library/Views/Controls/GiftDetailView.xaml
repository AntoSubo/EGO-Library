<UserControl x:Class="EGO_Library.Views.Controls.GiftDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:EGO_Library.Converters"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <UserControl.Resources>
        <converters:ImagePathConverter x:Key="ImagePathConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
            
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,10">
            <TextBlock Text="EGO Gift"
                       FontSize="20"
                       FontWeight="Bold"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Help" 
                        Width="60" 
                        Margin="0,0,5,0" 
                        Command="{Binding HelpCommand}"/>
                <Button Content="Back" 
                        Width="60" 
                        Command="{Binding GoBackCommand}"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="#2D2D30" 
                    BorderBrush="#404040" 
                    BorderThickness="1"
                    CornerRadius="8" 
                    Padding="20" 
                    Margin="0,0,10,0"
                    VerticalAlignment="Top">
                <Image Source="{Binding Gift.ImagePath, Converter={StaticResource ImagePathConverter}}" 
                       Width="120" 
                       Height="120"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Gift.Name}" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Margin="0,0,0,5"/>

                <TextBlock Text="{Binding Gift.Tier, StringFormat='Tier: {0}'}" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>

                <TextBlock Text="Status:" 
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Gift.Status}" 
                           Margin="0,0,0,10"/>

                <TextBlock Text="Effect:" 
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Gift.Effect}" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,10"/>

                <TextBlock Text="Description:" 
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Gift.Description}" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,10"/>
                <TextBlock Text="Sources:" 
                           FontWeight="SemiBold" 
                           Margin="0,10,0,5"/>
                <ItemsControl ItemsSource="{Binding Gift.Sources}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#404040" 
                                    CornerRadius="4" 
                                    Padding="5" 
                                    Margin="0,2">
                                <StackPanel>
                                    <TextBlock Text="{Binding Location}" 
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Type, StringFormat='Type: {0}'}" 
                                               FontSize="10" 
                                               Foreground="#CCCCCC"/>
                                    <TextBlock Text="{Binding DropRate, StringFormat='Drop Rate: {0:P1}'}" 
                                               FontSize="10" 
                                               Foreground="#CCCCCC"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>