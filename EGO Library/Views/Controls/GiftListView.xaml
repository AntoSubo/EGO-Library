<UserControl x:Class="EGO_Library.Controls.GiftListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:EGO_Library.ViewModels"
             Width="Auto" Height="Auto"
             Foreground="White" FontFamily="Segoe UI">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель фильтров -->
        <Border Grid.Column="0" Background="#FFF" CornerRadius="8" Padding="10" Margin="0,0,10,0" >
            <StackPanel>
                <TextBlock Text="Filters" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <!-- Статусы -->
                <TextBlock Text="Status" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <StackPanel>
                    <CheckBox Content="Burn" Margin="0,2"/>
                    <CheckBox Content="Bleed" Margin="0,2"/>
                    <CheckBox Content="Charge" Margin="0,2"/>
                    <CheckBox Content="Poise" Margin="0,2"/>
                </StackPanel>

                <!-- Уровень -->
                <TextBlock Text="Level" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox Width="100" SelectedIndex="0">
                    <ComboBoxItem Content="Tier 1"/>
                    <ComboBoxItem Content="Tier 2"/>
                    <ComboBoxItem Content="Tier 3"/>
                    <ComboBoxItem Content="Tier 4"/>
                    <ComboBoxItem Content="Tier 5"/>
                </ComboBox>

                <!-- Поиск -->
                <TextBox Margin="0,20,0,5" Height="25" />
                <Button Content="Search" Height="25" Width="100" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <!-- Правая область с карточками -->
        <DockPanel Grid.Column="1">
            <!-- Верхняя панель -->
            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <TextBlock Text="EGO Library"
                           FontSize="22"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
                <Button Content="Help"
                        Width="70"
                        Height="25"
                        Margin="10,0,0,0"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right"/>
            </DockPanel>

            <!-- Сетка карточек -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <WrapPanel Margin="0,10,0,0" ItemWidth="130" ItemHeight="150">
                    <!-- Пример одной карточки -->
                    <Border Background="White"
                            BorderBrush="#DDD"
                            BorderThickness="1"
                            CornerRadius="6"
                            Width="130"
                            Height="150"
                            Margin="5">
                        <StackPanel>
                            <Rectangle Height="80" Fill="#EEE" Margin="5"/>
                            <TextBlock Text="Gift Name"
                                       HorizontalAlignment="Center"
                                       FontWeight="SemiBold"
                                       Margin="0,5,0,0"/>
                            <TextBlock Text="Tier 3"
                                       HorizontalAlignment="Center"
                                       FontSize="12"
                                       Foreground="Gray"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</UserControl>
