<Window x:Class="EGO_Library.windowssall.Recipes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EGO_Library.windowssall"
        mc:Ignorable="d"
        Title="Recipes" Height="300" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="*"/>
            <!-- Основной контент -->
            <RowDefinition Height="Auto"/>
            <!-- Кнопка -->
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Recipes"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- Рецепты -->
        <ItemsControl Grid.Row="1"
                      ItemsSource="{Binding RecipeItems}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="80" Height="80"
                                Background="#FFF"
                                BorderBrush="#CCC"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="5,0">
                            <Image Source="{Binding ImagePath}" Stretch="Uniform"/>
                        </Border>

                        <!-- Добавляем "+" между элементами -->
                        <TextBlock Text="+"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="#888"
                                   VerticalAlignment="Center"
                                   Margin="5,0,5,0"
                                   Visibility="{Binding IsLast, Converter={StaticResource BoolToVisibilityInverseConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Кнопка Back -->
        <Button Grid.Row="2"
                Content="Back"
                Width="80"
                Height="30"
                HorizontalAlignment="Right"
                Command="{Binding BackCommand}"
                Margin="0,10,0,0"/>
    </Grid>
</Window>
